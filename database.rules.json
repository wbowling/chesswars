{
  "rules": {
    "codes": {
      "${uid}": {
        ".read": "auth.uid != null",
        ".write": "auth.uid == uid"
      }
    },

    ".read": "auth.uid != null",
    ".write": "auth.uid != null"
  }
}
